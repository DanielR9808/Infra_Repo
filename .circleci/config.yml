version: 2
jobs:
  build:
    docker: 
      - image: circleci/node:7.10 # the primary container, where your job's commands are run
    
    workiworking_directory: ~/repo
    steps:
      - checkout # check out the code in the project directory
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "package.json" }}
            - v1-dependencies  
      - run: yarn install
      

      - save_cache:
          paths:
            - node_modules
          key: v1-dependencies-{{ checksum "package.json" }}
  deploy:
    docker:
      - image: circleci/openjdk
    steps:
       - add_ssh_keys:
          fingerprints:
            - "87:ba:54:2b:83:c2:d6:0d:4c:01:23:09:8d:cc:2a:95"
       - checkout
       - run:
          name: "Ejecucion" 
          command: |
            
            ls --all
            pwd 
                     
            scp -o StrictHostKeyChecking=no ./index.html ec2-3-227-89-116.compute-1.amazonaws.com:/var/www/html

workflows:
  version: 2
  build-deploy: 
    jobs:
      - build
      - deploy:
          requires:
            - build
          filters:
            branches:
              only: master
